; Configuration applied to the SMTP agent.
[server]
name=  # Name of the server (not to be confused with hostname))
port=25
processes=  # Must be a single value or a comma-separated list of processes
interface=  # Name of the network interface to bind to, e.g., eth0, lo, etc.

[controller]
whitelist_commands=
